name: Maven Release CI

on: workflow_dispatch

jobs:
  release:
    permissions:
      contents: write
      packages: write
    uses: dbstar-org/general/.github/workflows/maven-release.yml@v2
    secrets: inherit